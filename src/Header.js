import React from "react";

const Header = function(){
    return (
        <div className="header" style={{textAlign:"center",background:"#000", color:'#fff', padding:20, fontSize:20,textTransform:'uppercase' }}>
            Phone Directory
        </div>
    )
}

export default Header